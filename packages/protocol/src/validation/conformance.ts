/**
 * Conformance validation — VirtualRegistry (stricter-than-mainnet)
 */
export {
  VirtualRegistry,
  VirtualRegistryError,
  type RegisterResult,
} from "../core/virtualRegistry/VirtualRegistry.js";
