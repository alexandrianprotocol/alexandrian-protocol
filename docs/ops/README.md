## Operational Runbooks

This folder documents operational details for running Alexandrian in production‑like environments.

- `MAINNET-ADDRESSES.md` — deployed contract addresses, key IPFS CIDs, and subgraph endpoints.

Use this folder as the single source of truth for:

- Where the live contracts and subgraphs are deployed.
- Which artifacts (IPFS, seeds) correspond to which Knowledge Blocks.

For verification flows, see `../VERIFY-M1.md` and the root `README.md` quick links.

